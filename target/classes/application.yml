server:
  port: 8080

spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:mysql://localhost:3306/duks-coffee
    username: duks
    password: duks123456789
  jpa:
    show-sql: true
    hibernate:
      ddl-auto:  update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect